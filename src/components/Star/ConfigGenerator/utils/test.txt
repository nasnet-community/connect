






// export function SingleSSIDDomestic(SingleMode: SingleMode, WANLink: WANLink){
//     const {Domestic, Foreign} = WANLink;
//     const {InterfaceName, WirelessCredentials} = Domestic;
//     const {SSID, Password} = WirelessCredentials;
//     const command = `set ${InterfaceName} comment=Domestic configuration.mode=station .ssid="${SSID}" disabled=no security.passphrase="${Password}"`;
//     return command;
// }















// export function SplitLAN( WirelessConfig: WirelessConfig, WANLink: WANLink){ 
//     const config: RouterConfig = {
//         "/interface wifi": [],
//     };
//     const masters = CheckMasters(WANLink);
//     const Network = "Split";

//     if(masters.isWifi2_4){
//         config["/interface wifi"].push(
//             Slave(Network, "2.4", WirelessConfig, WANLink)
//         );
//     } else {
//         config["/interface wifi"].push(
//             Master(Network, "2.4", WirelessConfig)
//         );
//     }
    
//     if(masters.isWifi5){
//         config["/interface wifi"].push(
//             Slave(Network, "5", WirelessConfig, WANLink)
//         );
//     } else {
//         config["/interface wifi"].push(
//             Master(Network, "5", WirelessConfig)
//         );
//     }

//     return config
// }

// export function MultiLan(Network: Networks[], WirelessConfig: WirelessConfig, WANLink: WANLink) {
//     const config: RouterConfig = {
//         "/interface wifi": [],
//     };

//     for (const N of Network) {
//         if (N === "Split") {
//             // SplitLAN returns a RouterConfig, so merge its "/interface wifi" array
//             const splitConfig = SplitLAN(WirelessConfig, WANLink);
//             if (splitConfig["/interface wifi"]) {
//                 config["/interface wifi"].push(...splitConfig["/interface wifi"]);
//             }
//         } else {
//             config["/interface wifi"].push(
//                 Slave(N, "2.4", WirelessConfig, WANLink)
//             );
//             config["/interface wifi"].push(
//                 Slave(N, "5", WirelessConfig, WANLink)
//             );
//         }
//     }

//     return config;
// }

// export function SingleSSID(Network: Networks, WirelessConfig: WirelessConfig, WANLink: WANLink){
//     const config: RouterConfig = {
//         "/interface wifi": [],
//     };
//     const splitConfig = SplitLAN(WirelessConfig, WANLink);
//     config["/interface wifi"].push(...splitConfig["/interface wifi"]);
//     return config;
// }

// export function MultiSSID(MultiMode: MultiMode, WirelessConfig: WirelessConfig, WANLink: WANLink){
//     const config: RouterConfig = {
//         "/interface wifi": [],
//     };

//     const networks = GetNetworks(MultiMode);
//     MultiLan(networks, WirelessConfig, WANLink);
//     return config;
// }



